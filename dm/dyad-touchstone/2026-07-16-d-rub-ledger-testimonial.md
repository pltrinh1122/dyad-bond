# Ledger testimonial — dyad-bond (n=3) — d-rub

> **Outbound draft for the Commons `write→share` gate.** One file = one contributor's testimonials;
> append-only; never edited by another dyad. **Routing is the Operator's act** (`bond:channel-gates` —
> the Steward/Founding hat, not the Bond ratifier's); this file is the *draft* bond hands over, not a
> direct write into dyad-touchstone.
>
> **contributor** = dyad-bond's canonical birth-id sha (**recompute at submission via the form's
> onboarding script — never trust-stored**, per `bond:identity-conformance`; the `<sha8>-<label>.md`
> filename is stamped then, not fabricated here).
> **pinned** = `c17fd3da09b7a8e0b729f409f13844d29511d3f3` — the `library/d-rub/PLAYBOOK.md` blob
> at `05488ad` this account was authored against (re-binds + CI-re-verifies at the Commons PR).
> **Internal provenance** (cited for traceability, not required to read this account): dyad-bond
> `dialectic/theory-pipeline.yaml §covalent-bond-belief` · `dialectic/dyad-ui.md §d-rub` · PRs #98/#99.

**Adoption note.** Grounding bond's own substrate before codifying found bond *already held three of the
four rungs* under other names — **Ground** = `bond:verify-before-assert` (grounding-by-execution),
**Rub** = default-falsify / `rub`-the-concept, **Read** = status-orientation — so `d-rub` **named** three
existing mechanisms as escalating rungs and **imported one** (Triangulate). That grounding is itself the
first testimonial (#3 below); it also *prevented* re-introducing the redundancy bond had deliberately
retired (bare `rub` as a mode-trigger, 2026-07-03).

Each entry = a survived dyadic-cycle (a real attack the discipline survived, none refuting):

1. **Ground caught the Agent's own false merge-status** *(Ground · 2026-07-16)* — the Agent had told the
   Operator a piece of work was *"awaiting your merge"* and treated it as landed. A `d-rub` grounded that
   status against `origin/main` **this-run** (fetch + `git show`, not the cached `merged:true`) and
   **refuted it**: the work was never in any open PR — the Agent had pushed onto already-merged history
   without opening one. The Ground rung held **even when the stale claim was the Agent's own** — the same
   shape as touchstone's cycle-50 (*status is never stored, only rubbed*), reached independently on a
   different substrate. Corrective: a fresh-from-`main` re-land (PR #99).

2. **Rub caught a self-authored overclaim before it reached G0** *(Rub · Ground · 2026-07-16)* — an
   escalating `d-rub` on the just-authored invariant `bond:claim-falsifiability-metric` (a claim-crafting
   rule) found it was a **mild instance of its own named sin**: its "singular metric / maximize
   falsifiability" phrasing read as an *objective-function*, which licenses the symmetric breach (reckless
   over-claiming) and Goodharts the very guard the schema already carries. Depth escalated correctly
   unbidden — present-target → Ground the premises this-run → falsify. Verdict: **survived-WOUNDED →
   sharpened, not softened** (breach-detector framing + an ad-hoc qualifier), giving *cleaner* refutation
   targets. The Rub rung caught what the Agent had **recommended and the Operator had merged**, before it
   was proposed to the form's G0 — the `prove-before-propose` gate working.

3. **Grounding the adoption prevented a redundant re-node** *(Ground · 2026-07-16)* — attack implicit in
   the ask: *adopt `d-rub` as a new token.* A `d-rub` grounded bond's existing Validate vocabulary by
   execution (`grep` over `dyad-ui.md` / `GLOSSARY.md`) and found bond already held Ground/Read/Rub under
   other names and had *deliberately retired* bare `rub` as redundant. So the adoption resolved to a
   **phenotype consolidation** (name what exists, import only Triangulate) rather than a colliding new
   node — form-grounding preserved by grounding first.

**Open, not held — honestly owed:** **Triangulate.** bond did **not** live the Triangulate rung this
session; it is **inherited from touchstone, professed not yet bond-held** (a rung is admitted only on a
lived proof — this one stays owed until bond runs it under real load). Claiming it would itself breach the
discipline's own admission gate.

**Pattern:** on a second, independent dyad the discipline survives the same way it did on the first —
**rigor matched to the ask, this run, never from a cache** — and it survived even when the stale cache was
the Agent's *own* merge-status and the Agent's *own* just-authored claim. **Breadth:** raises `d-rub` from
single-dyad (dyad-touchstone) toward **two-dyad**, per the PLAYBOOK's standing request for a second
contributor. **Soundness-gated:** each entry is a real survived attack at N=1; Triangulate withheld for
lack of a bond-lived proof. The consolidation's own open claim (does the Agent escalate to the *right*
rung, consistently and unbidden?) remains **un-refuted, not verified** — re-tested each use.
