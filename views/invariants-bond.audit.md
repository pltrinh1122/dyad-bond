# INVARIANT AUDIT VIEW — side-by-side fidelity check  *(DERIVED · regenerate, never edit)*

> **⚠ DERIVED + GENERATED by `bin/audit_view.py` — do NOT edit, regenerate.** The E1-fidelity
> instrument (→ `dialectic/dyad-md-yaml-regen.md §Change-propagation`). Per node: the shadow's
> `one_liner` (the **derived** claim) beside the **verbatim** `DYAD.md` `source_quote` it extracts.
> The **machine gate** certifies each quote is *present* in `DYAD.md` (`✓`/`✗`); **you** audit whether
> the claim faithfully *paraphrases* it — the slice no machine can certify. Cite the source, not this view.

**Gate:** 13/13 source_quotes verbatim-present in DYAD.md · ✓ all present

### `bond:craft-telos`  (✓)
- **derived** — shadow `one_liner`: The dyad's work stays oriented toward the lived interior craft of the dyadic relationship — the relationship itself, not an external artifact, is the craft-object.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#craft-telos`): The **lived interior craft of the dyadic relationship**

### `bond:C1`  (✓)
- **derived** — shadow `one_liner`: The bond is kept covalent — sharing not transfer, two distinct models not merged.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#craft-value`): *sharing not transfer, two distinct models not merged.*

### `bond:two-models`  (✓)
- **derived** — shadow `one_liner`: Two distinct, genuinely independent models are maintained — the second perspective that makes falsification real, and its loss to a single merged model is meld.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#craft-value`): an independent second model, else the falsification is meld-counterfeit. IFF1.

### `bond:anti-cave`  (✓)
- **derived** — shadow `one_liner`: The Agent manufactures genuine grounds for the Operator to dissent before any disposition surface — an ungrounded surface that can induce a wrong Y is the breach.
- **source** — `DYAD.md` `source_quote` (node home `dialectic/relationship-craft.md#the-anti-cave-duty`): the agent grounds the frame so the human has real grounds to dissent, else a wrong Y is induced.

### `bond:single-home`  (✓)
- **derived** — shadow `one_liner`: Each fact lives in exactly one content-home and is restated in no other file.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Single-home`): each fact lives in exactly one content-home, restated in no other file

### `bond:kb-graduation`  (✓)
- **derived** — shadow `one_liner`: A learning is in kb only after it has survived falsification in dialectic.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Single-home`): a learning is in `kb/` only **after** it survives falsification in `dialectic/`.

### `bond:form-grounding`  (✓)
- **derived** — shadow `one_liner`: The form's G0 is inherited unmodified (extend, never redefine).
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Form-grounding`): the Frame's **G0**, unmodified: extend, never redefine.

### `bond:prove-before-propose`  (✓)
- **derived** — shadow `one_liner`: A local evolution is proposed back to the form only after it has been proven here.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Form-grounding`): evolve locally → prove here → only then propose back via the Founding-Operator gate.

### `bond:channel-gates`  (✓)
- **derived** — shadow `one_liner`: A disposition reaches only the Operator hat that owns it — Bond (proposer/ratifier), Founding (the form gate), or Steward (cross-dyad intake) — with no hat over-reaching another's seat.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Channel-discipline`): A disposition reaches only the hat that owns it; no hat over-reaches another's seat.

### `bond:DFD`  (✓)
- **derived** — shadow `one_liner`: A decision-framing carries a genuine non-strawman anti-thesis and resolves to a synthesis.
- **source** — `DYAD.md` `source_quote` (node home `kb/dfd.md`): A decision-framing carries a genuine non-strawman anti-thesis and resolves to a synthesis

### `bond:no-self-ratify`  (✓)
- **derived** — shadow `one_liner`: No candidate is ratified by its own proposer — the disposing half is never the proposing half.
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#craft-value`): disposer ≠ proposer, else the candidate is self-graded.

### `bond:identity-conformance`  (✓)
- **derived** — shadow `one_liner`: The running anchor's identity matches the frozen birth-id sha, recomputed from CLAUDE.md@1ab6ad0 and never trust-stored (drift from the canonical sha is the breach).
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Frame`): drift from the canonical birth-id sha = breach; the sha is computed over the original add-commit `1ab6ad0`

### `bond:glossary-conformance`  (✓)
- **derived** — shadow `one_liner`: Every term used in an invariant node is defined in GLOSSARY.md (an undefined orphan term is the mechanical breach).
- **source** — `DYAD.md` `source_quote` (node home `DYAD.md#Frame`): **The core below is written in this vocabulary**

