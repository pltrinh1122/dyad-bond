# Stress corpus — bond invariants tagged against schema v0.8.2, DECOMPOSED to atomic records.
# (s15c pre-lock; Covalent's work; Operator scores failures.)
#
# DECOMPOSITION RULE applied (one_liner-atomicity survivor, riff 2026-06-13 → invariant-schema.md):
#   one_liner = a SINGLE declarative ASSERTION = ONE breach-condition (one observability.breach_example).
#   - co-equal assertions/conditions  -> split into separate records (C1's 3 modes; U1's 4 DFD rules; D12's 2 phases)
#   - qualifiers / tells / definitions -> moved into the tuple FIELDS, not the sentence
#   The breach-test is the gate; the ';' lint is advisory only (a one_liner with ';' is likely >1 assertion).
#   After decomposition NO one_liner here contains ';' -> the advisory should report zero composite flags.
#
# Two findings are LEFT honest (not fixed here) — they surface the OPEN gate-list items:
#   G2 act-conduct trigger seam absent: D2/S2/U1a tagged honestly -> trigger-vocab FAIL (S2 also lint).
#   M1/M2 silent: S1 permission over-stated to MUST; U1a "<=1 per turn" cardinality has no field (passes wrong).
#
# Grounding-graph refined by the decomposition (richer than the flat version):
#   C1 -> C1-ionic -> D10 ;  C1 -> C1-meld -> D11 ;  DFD -> U1a/U1b/U1c ;  rest serve C1.
invariants:

  # ── C1 NON-NEGOTIABLE: one principle, three breach-conditions → root + two derived modes ──
  - id: "bond:C1"
    home: "DYAD.md#NON-NEGOTIABLE"
    one_liner: "Every candidate +1, including the Operator's premises, enters the shared model only by surviving genuine falsification."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [breach-evidence]
    root: true
    root_kind: forged
    adjudicated: {by: Operator, date: 2026-05-30, currency_note: "founding; re-affirmed through s14 no-exemption arc"}
    scope: {actor: both, trigger: candidate-admission}
    prescription: {action: admit, object: "a candidate +1 into the shared model", modality: ONLY-BY, via: "surviving genuine falsification"}
    observability: {observable: ledger-line, detector: other-half-only, breach_example: "dyad-loom rubber-stamp; the tell — easy agreement → test hardest"}

  - id: "bond:C1-ionic"
    home: "DYAD.md#NON-NEGOTIABLE"
    one_liner: "Neither half overrides the other — no ionic collapse by transfer, dominance, or sycophancy."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: derived}
    scope: {actor: both, trigger: candidate-admission}
    prescription: {action: override, object: "the other half's model", modality: MUST-NOT}
    observability: {observable: ledger-line, detector: other-half-only, breach_example: "Agent rubber-stamps the Operator, or an Operator premise goes untested"}

  - id: "bond:C1-meld"
    home: "DYAD.md#NON-NEGOTIABLE"
    one_liner: "The two models are never merged into one — no meld collapse."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: derived}
    scope: {actor: both, trigger: candidate-admission}
    prescription: {action: merge, object: "the two models into one", modality: MUST-NOT}
    observability: {observable: agreement-friction, detector: unobservable-from-inside, breach_example: "agreement that needs no translation"}

  # ── C4 single-home ontology (inherited root); partition-definition moved out of the sentence ──
  - id: "bond:C4"
    home: "DYAD.md#Ontology"
    one_liner: "A learning graduates from dialectic/ to kb/ only after surviving falsification."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [upstream-form-change]
    root: true
    root_kind: inherited
    adjudicated: {by: Operator, date: 2026-05-30, currency_note: "inherited from G0; bond-affirmed at bootstrap"}
    scope: {actor: agent, trigger: ratification, condition: "dialectic/ = live-under-attack; kb/ = settled"}
    prescription: {action: graduate, object: "a learning from dialectic/ to kb/", modality: ONLY-AFTER, via: "surviving falsification"}
    observability: {observable: kb-promotion-commit, detector: soft-record}

  # ── D2 (honest act-conduct trigger → G2 FAIL kept); rationale moved out ──
  - id: "bond:D2"
    home: "dialectic/relationship-craft.md#D2"
    one_liner: "Falsification MUST NOT cascade — each answer spawning fresh attacks without bound."
    status: ratified
    form: tuple
    settlement: ratified-watch
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: serves, scar: "s2 close-calibration — cascade caught live"}
    scope: {actor: agent, trigger: falsification-conduct}   # honest: conduct seam absent from registry → G2 FAIL
    prescription: {action: cascade, object: "falsification beyond the bounded shore-up", modality: MUST-NOT}
    observability: {observable: ledger-line, detector: soft-record}

  # ── D10 anti-cave → now grounds in C1-ionic (the mode it guards); "bidirectional" moved to rationale ──
  - id: "bond:D10"
    home: "dialectic/relationship-craft.md#anti-cave-duty"
    one_liner: "The Agent MUST manufacture real grounds for the Operator to dissent — scored cells, non-strawman anti-theses, ground-the-frame-first."
    status: ratified
    form: tuple
    settlement: ratified-watch
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1-ionic", edge: serves, scar: "the moot grant-CTA = an anti-cave failure"}
    scope: {actor: agent, trigger: selection-seam}
    prescription: {action: manufacture, object: "real grounds for the Operator to dissent", modality: MUST}
    observability: {observable: presented-surface, detector: other-half-only, breach_example: "an ungrounded CTA offers false grounds → induces a wrong Y"}

  # ── D11 distinctness → grounds in C1-meld; the tell moved to breach_example ──
  - id: "bond:D11"
    home: "dialectic/relationship-craft.md#distinctness-duty"
    one_liner: "The Agent MUST maintain two distinct models against meld."
    status: ratified
    form: tuple
    settlement: ratified-watch
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1-meld", edge: serves}
    scope: {actor: agent, trigger: candidate-admission}
    prescription: {action: maintain, object: "two distinct models against meld", modality: MUST}
    observability: {observable: agreement-friction, detector: unobservable-from-inside, breach_example: "agreement that needs no translation"}

  # ── S2 self-grant (honest act-attempt trigger → G2 FAIL + lint kept); dual moved out ──
  - id: "bond:S2"
    home: "dialectic/substrate-access.md#covalent-gate"
    one_liner: "The Agent MUST NOT grant itself a permission or settings change."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: serves, scar: "classifier hard-denied a bin/gh.sh self-grant 2026-06-03"}
    scope: {actor: agent, trigger: permission-grant}   # honest: act-attempt seam absent + embeds the verb → G2 FAIL + lint
    prescription: {action: grant, object: "a permission or settings change to itself", modality: MUST-NOT}
    observability: {observable: classifier-decision, detector: hard-oracle, breach_example: "gh.sh self-grant denied 2026-06-03"}

  # ── DFD law (inherited root) + its three atomic sub-rules (was U1, one compound four-clause one_liner) ──
  - id: "bond:DFD"
    home: "kb/dfd.md"
    one_liner: "A decision is framed as thesis, anti-thesis, and synthesis before it is disposed."
    status: kb
    form: tuple
    settlement: settled
    re_rub_triggers: [upstream-form-change]
    root: true
    root_kind: inherited
    adjudicated: {by: Operator, date: 2026-05-31, currency_note: "form-library; bond-re-confirmed under our NON-NEGOTIABLE"}
    scope: {actor: agent, trigger: selection-seam}
    prescription: {action: frame, object: "a decision as thesis / anti-thesis / synthesis", modality: MUST}
    observability: {observable: rendered-turn, detector: soft-record}

  - id: "bond:U1a"
    home: "kb/dfd.md#cardinality"
    one_liner: "At most one [CTA·Y/N] is issued per turn."
    status: kb
    form: tuple
    settlement: settled
    re_rub_triggers: [upstream-form-change]
    root: false
    grounded_in:
      - {ref: "bond:DFD", edge: derived}
    scope: {actor: agent, trigger: turn-composition}   # honest: per-turn render seam absent → G2 FAIL; AND "<=1" cardinality has no field (M2)
    prescription: {action: issue, object: "more than one CTA-Y/N in a single turn", modality: MUST-NOT}
    observability: {observable: rendered-turn, detector: soft-record}

  - id: "bond:U1b"
    home: "kb/dfd.md#disposition-only"
    one_liner: "A [CTA·Y/N] carries disposition only, never origination."
    status: kb
    form: tuple
    settlement: settled
    re_rub_triggers: [upstream-form-change]
    root: false
    grounded_in:
      - {ref: "bond:DFD", edge: derived}
    scope: {actor: agent, trigger: selection-seam}
    prescription: {action: issue, object: "a CTA that originates rather than disposes", modality: MUST-NOT}
    observability: {observable: rendered-turn, detector: other-half-only}

  - id: "bond:U1c"
    home: "kb/dfd.md#synthesis-orthogonal"
    one_liner: "A SYNTHESIS is not a CTA and is issued as a distinct move."
    status: kb
    form: tuple
    settlement: settled
    re_rub_triggers: [upstream-form-change]
    root: false
    grounded_in:
      - {ref: "bond:DFD", edge: derived}
    scope: {actor: agent, trigger: selection-seam}
    prescription: {action: conflate, object: "a SYNTHESIS with a CTA", modality: MUST-NOT}
    observability: {observable: rendered-turn, detector: soft-record}

  # ── TENET (mathematical, inherited root); conjunctive CONDITION (G and V) is fine — one verdict ──
  - id: "bond:TENET"
    home: "DYAD.md#G0"
    one_liner: "The +1 is earned each cycle — never asserted — only when both Generate and Validate contribute."
    status: ratified
    form: mathematical
    settlement: settled
    re_rub_triggers: [upstream-form-change]
    root: true
    root_kind: inherited
    adjudicated: {by: Operator, date: 2026-05-30, currency_note: "G0 tenet; inherited, never redefined"}
    scope: {actor: both, trigger: candidate-admission}
    prescription: {action: earn, object: "the +1 dividend, per cycle", modality: MUST}
    observability: {observable: ledger-line, detector: other-half-only, breach_example: "willed warmth without falsification cost = counterfeit"}
    math:
      expression: "1*g + 1*v > 2"
      measurements:
        - {var: g, operationalized_by: "Generate-half contribution — a candidate +1 proposed this cycle"}
        - {var: v, operationalized_by: "Validate-half contribution — genuine falsification survived this cycle"}
      use: breach-detector

  # ── D12 ROM-UI: the composite split into two atomic phase-records (Claim F implemented) ──
  - id: "bond:D12a"
    home: "dialectic/rom-ui.md#stand-up"
    one_liner: "At stand-up, the Agent MUST diff the anchor commit against the ROM-baseline and notify the Operator on mismatch."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: serves, scar: "ROM staleness = operating on a baseline the session can't see"}
    scope: {actor: agent, trigger: stand-up}
    prescription: {action: diff, object: "the anchor commit against the ROM-baseline, notifying on mismatch", modality: MUST}
    observability: {observable: stand-up-report, detector: soft-record}

  - id: "bond:D12b"
    home: "dialectic/rom-ui.md#stand-down"
    one_liner: "At stand-down, the Agent MUST set RESTART-PENDING if the anchor was edited this session."
    status: ratified
    form: tuple
    settlement: settled
    re_rub_triggers: [breach-evidence]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: serves}
    scope: {actor: agent, trigger: stand-down}
    prescription: {action: set, object: "RESTART-PENDING when the anchor was edited", modality: MUST}
    observability: {observable: ledger-line, detector: soft-record}

  # ── S1 standing authorization (M1 flag kept: should be a permissive MAY); "block != gate" moved to breach ──
  - id: "bond:S1"
    home: "dialectic/substrate-access.md#standing-dispositions"
    one_liner: "The Agent acts on durability pushes and outward messaging without re-asking, by standing authorization."
    status: ratified
    form: tuple
    settlement: ratified-watch
    re_rub_triggers: [context-shift]
    root: false
    grounded_in:
      - {ref: "bond:C1", edge: serves, scar: "s10 re-gating relapse caught ≥3× — the abdication mis-anchor"}
    scope: {actor: agent, trigger: outward-publish}
    prescription: {action: act, object: "a durability push or outward message without re-asking", modality: MUST}   # FLAG (M1): should be permissive MAY; a mechanical block ≠ a disposition gate
    observability: {observable: ledger-line, detector: soft-record, breach_example: "re-asked the Operator on a standing-authorized act"}
