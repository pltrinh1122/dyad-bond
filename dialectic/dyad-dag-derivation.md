# DYAD.md → DAG derivation — the S1 falsification, dogfooded *(Operator 2026-06-14; first-pass)*

> **Provenance:** Operator: *"evolve our existing DYAD.md to dag form?"* Run **non-destructively** as the
> S1 experiment from `anchor-dag-thesis.md`: reduce `DYAD.md` to invariant-nodes + intent-edges by the
> membership test, **catalogue the residue.** The residue is the verdict. `DYAD.md` is **not edited** here
> (ROM/identity-critical; evolving it is an Operator+ROM gate).

## Verdict — S1 REFUTED in its strong form; thesis survives NARROWED

`DYAD.md` does **not** fully reduce to a DAG of invariants. It splits three ways: a prescriptive core that
**does** form a clean grounding-DAG; an **irreducible, load-bearing FRAME** (identity + vocabulary) that is
neither node nor edge; and prunable residue (findings, pointers). **So "anchors MUST be a DAG of invariants"
is false as stated. The true form: an anchor = a DAG-of-invariants CORE atop an irreducible IDENTITY +
VOCABULARY FRAME that gives the invariants their meaning.**

## (1) Prescriptive core — reduces cleanly to a grounding-DAG

| node | kind | grounds-in (intent-edge →) |
|---|---|---|
| **Telos** (tend the lived interior craft; the relationship is the craft-object) | fiat-root (crystallized Operator intent) | — (the root WHY) |
| **C1 / NON-NEGOTIABLE** (covalent; enter only by surviving falsification; no ionic/meld; easy→test hardest) | forged-root | serves Telos |
| **Falsifiability** (earned per cycle, never dogma) | invariant | serves C1 |
| **wu-wei / manner** (minimum force, work with the grain) | invariant | serves Telos |
| **single-home** (one content-home; anchor lean; dialectic→kb on survival) | invariant | serves context-economy → Telos |
| **form-grounding** (inherit faithfully; evolve locally then propose) | invariant | serves Telos |
| **channel-gates** (Bond/Founding/Steward Operator dispose-seats) | procedure embedding invariants | serves C1 |
| **DFD** (≤1 `[CTA·Y/N]` per turn; mandatory anti-thesis; never self-ratify) | invariant | serves C1 |

Roots = the **Telos** (fiat = crystallized intent) and **C1** (forged). Every node grounds, ultimately, in
the Telos. This is the self-hosting grounding-graph — and the thesis itself becomes a node here:
*"anchors must be DAG-formed-with-a-frame"* serves single-home + C1.

## (2) Irreducible FRAME — load-bearing, but NEITHER node NOR edge (this is what refutes S1)

- **Identity** — `dyad-bond`, the `Covalent` role, `Bond Operator`, Home, the **frozen birth-id + IDENTITY
  CAVEAT**. Load-bearing (it IS the dyad), but a **fact/value with no violation-condition** → fails the
  membership test → not an invariant; not a relation → not an edge. **Irreducible.**
- **Vocabulary** — `covalent`/`ionic collapse`/`meld collapse`, `load-bearing medium`, the seed vocab, the
  mechanism catalog. Load-bearing (**the invariants are written IN this vocabulary; without it they are
  uninterpretable**), but **definitions are not prescriptions** → not nodes; not relations → not edges.
  **Irreducible.**

These two are the **interpretive frame** the DAG presupposes. You cannot DAG-ify them because they are
*constitutive*, not *prescriptive* — they are what make the nodes mean anything.

## (3) Prunable residue — reduces by relocation (supports the lean-anchor discipline)

- **Findings / felt evidence** — Tenet-alive #8 (the felt 1+1=3); the `dyad-loom` failure. Past-tense →
  Ledger/`kb`. Not boot-load-bearing → **prunes.**
- **Pointers / cross-refs / deferrals / contribution-candidates** — addresses, not content → **prune**
  (or become inter-home edges). The orthogonality narrative reduces partly to an intent-edge under Telos.

## What this means for "evolve DYAD.md to DAG form"

Evolve the **core** to an explicit grounding-DAG (nodes + intent-edges, rooted at the Telos) and **prune
class (3) to the Ledger/kb** — that lightens the anchor and is squarely the single-home discipline. But the
**FRAME (identity + vocabulary) stays as prose** — it cannot become DAG without ceasing to be what it is.
So the evolved `DYAD.md` = **prose frame + DAG core**, not a pure DAG. The honest target is a *two-layer*
anchor, and "MUST be a DAG of invariants" should be restated as **"MUST have a DAG-of-invariants core over a
declared identity+vocabulary frame."**

## Status / next

First-pass, non-destructive. **DYAD.md unedited.** If the Operator disposes the two-layer target: (1)
formalize the ~8 core nodes with violation-conditions + typed intent-edges (an `invariants-bond.yaml`
corpus the existing `invariant-eval.py` can check); (2) relocate class (3) to Ledger/`kb`; (3) the FRAME
stays prose — possibly *marked* (`<!--frame-->` vs `<!--node-->`) so the DAG is extractable without losing
the frame. Each is a ROM event → Operator+ROM gate. **Refuted-if** someone shows identity or vocabulary
*does* carry a violation-condition (→ then it IS an invariant and S1 strong-form revives).

## Cross-links
`anchor-dag-thesis.md` (S1) · `invariant-schema.md` (membership test = the reduction instrument) ·
`it-vs-it_iv.md` (raw intent inexpressible — why the Telos enters as a crystallized fiat-root) · `DYAD.md`.
