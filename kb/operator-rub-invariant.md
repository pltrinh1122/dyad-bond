---
locus: phenotype   # settled craft — kb-with-caveat (survives, not settled — see Coverage below). Key → MAP.md.
---

# The Operator-Rub Invariant — interior findings are debt until rubbed

*Settled knowledge, kb-with-caveat. Authored `dialectic/relationship-craft.md` (s14, 2026-06-11) as
"the dispensation arc's survivor: riff → raff → rub-back → ratification-check → dispose" — this file
holds the graduated core; the dialectic entry collapses to a pointer here. The later, still-open
threads that accumulated under the same heading (the `C_locus(t)` steering model, the `I↔In_variant`
cycle, the eureka-tachometer, the Telos-`U`-projection reframe, dyad sovereignty) did **not** graduate
with this and remain in `dialectic/relationship-craft.md` as candidate.*

## What it governs

The division of labor that lets the Operator focus on the interaction model while the Agent carries
administrative + experimental operations — without the dispensation quietly becoming
falsification-debt (the counterfeit-flat failure this dyad's own PR #67 named at portfolio scale).

**The partition it rests on:** "what is truly needed of the Operator" splits into two residues — the
**epistemic** (where the Operator is the only second model in existence: felt-report, intent and its
clarification, contesting the Agent's coding of them, the reward-gradient watch) and the **sovereign**
(consent acts that can't be self-granted: grants, hooks, hat-work — one-time/rare, never a focus cost).
The epistemic functions *are* the interaction model; everything administrative/experimental sits
outside both residues, given a floor of revocability + visibility.

## The invariant — 3 conditions

1. **Scope.** The Operator's rub is required only for the **interior-evidence class** — findings whose
   evidence is the Operator's interior or behavior. Exterior/cross-dyad claims don't require it.
2. **Graduation gate (on findings, not on this invariant).** An interior finding is debt — provisional,
   willed-not-earned — until rubbed. While unrubbed: no `kb/` promotion, no citing-as-settled, no
   load-bearing use in further building.
3. **Debt-flatness.** The ratified-unrubbed / leaned-on-unrubbed count must not rise while the
   Operator's attention narrows to the interaction model. Rising debt under narrowing attention is
   counterfeit acceleration, regardless of the yield curve.

**Excluded by design:** the mechanics of *how* a rub happens (on manifested ledger artifacts,
minutes-per-session) — that's implementation, not a condition.

## Falsification status — survives, not settled

**Cleared its own gate once, completely, with real casualties.** At ratification (2026-06-11) the
debt register opened with 7 items (T1–T5, the residue partition, a clarity-event tag). Each went
through individual, adversarial disposition — not rubber-stamped:

- **5 graduated** on their own merits (T1-scoped to its mechanical form only; the residue partition,
  amended after a real gap was found; a clarity-event re-tag; T2, with a caveat attached; T3, which
  retired an Agent overreach).
- **1 retired as moot** — Operator-called, per an explicit rider that mootness-exits must be
  Operator-called, never Agent-initiated (else "moot" becomes a way to drain debt without paying rubs).
- **1 closed with an Operator amendment that refused an exemption the Agent had proposed** —
  *"nothing of the Operator's is exempt... a rubbed intent is a cached verdict going stale as the
  world shifts; fix = re-rub at context-shifts."*

Result: **debt-zero at first-pass close.** The invariant didn't just survive — it produced a real `N`
and a real course-correction along the way, which is the strongest evidence a falsification-gate can
generate (an easy, uncontested pass would be the weaker signal).

**Coverage gap, named not hidden:** all evidence is intra-dyad, same-agent — no cross-dyad or
cross-human attack has tested this invariant. Per `theory-pipeline.yaml`'s `graduation_rule`, this caps
at `survives`, not `settled` (`kb-with-caveat`).

**A second, distinct caveat — the Operator's own, on record at ratification:** *"I'm currently learning
and building my own discernment and discrimination skill... I can only learn through the
well-established scientific method built on falsification."* This qualifies every `Y` in the first
pass: the dispositions are real (the gate fired), but the contest-depth behind each one is bounded by a
self-declared in-training discrimination. Cite this invariant's evidence with that qualifier attached,
not smoothed over.

## Forward

Re-open if a fresh debt-register pass surfaces a finding the invariant's 3 conditions don't cleanly
route. The `L2b` gap closes only via a genuine cross-dyad/cross-human rub — not by more intra-dyad
passes, however rigorous.
