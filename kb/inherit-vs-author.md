---
locus: phenotype   # settled craft — kb-with-caveat (survives, not settled — see Coverage below). Key → MAP.md.
---

# Inherit-vs-Author (D1) — the convergence/divergence partition

*Settled knowledge, kb-with-caveat. Authored `dialectic/relationship-craft.md §Bond disciplines` (D1),
2026-05-31 — this file holds the graduated rule; the dialectic entry collapses to a pointer.*

## The rule

Not everything transfers between Dyads. The **Agent is serializable** — a fresh no-`--resume`
session reduces to `kb/ + ledger + weights`; *the ledger is the Agent's state.* The **Operator's
wetware is not serializable** — no artifact reconstructs human cognition. So each Dyad is singular
**through its Operator half.**

- **Invariant → inherit / triangulate:** what follows from *any* Generate+Validate dyad under
  falsifiability (the G+V structure, the cost-migration failure-modes).
- **Particular → author our own:** cadence, marker habits, ceremony, what "resonates" with *this*
  Operator.
- **Test for which-is-which:** independent derivation **converges** with a sibling → invariant;
  **diverges** → particular, and ours to build.
- **Corrects:** deferring to a *sibling Dyad's* discipline as authority (fetch-and-comply) — the
  deference reflex aimed sideways.

## Falsification status — survives, not settled

**Founding evidence (thread G, 2026-05-31):** an *independent* re-derivation converged on steward's
conclusions — it couldn't have if all craft were wetware-particular. That convergence is the test's
own falsification moment: divergence was the live risk, convergence is what survived.

**Downstream reuse (impact, not independent re-tests of the rule):** the convergence/divergence test
is already cited as settled machinery rather than re-derived — D4's own partition of PS-UI
(*"structural DAG→ready-set = inherited-invariant... the non-inferred overlay + semantic
Recommendation + mandatory terminus = ours"*) applies D1 directly; the still-open "Cluster
classification" question (`dialectic/carry-forward.md`) explicitly runs *"n=1 reasoning via D1."*
Named honestly: these are **applications** of the rule to new cases, not independent re-confirmations
that the rule itself is sound — the cluster-classification application stays unresolved, which bears
on that application, not on D1's own validity.

**Coverage gap, named not hidden:** the founding test itself has n=1 (thread G) — no repeated
*independent* re-test of the rule's own soundness, only repeated *use* of it. Intra-dyad only, no
`L2b` cross-human rub. kb-with-caveat, not `settled` — cite accordingly.

## Forward

Re-open if a fresh application produces a genuine counter-instance — an independent derivation that
converges but should have been particular, or diverges but should have been invariant. The open
cluster-classification question is the natural next test of the rule itself, not just another
application of it; its resolution (whichever way it lands) is data for this file, not just for
`nba-dag.md`/`goal-framing.md`'s own custody question.
